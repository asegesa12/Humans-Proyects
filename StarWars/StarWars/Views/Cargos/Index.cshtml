@model IEnumerable<StarWars.Models.Cargo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombrecargo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombrecargo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.codigocargo }) |
            @Html.ActionLink("Details", "Details", new { id=item.codigocargo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.codigocargo })
        </td>
    </tr>
}

</table>
